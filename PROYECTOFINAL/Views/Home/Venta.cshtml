@using PROYECTOFINAL.Models
@model int

    @{
        ViewBag.Title = "Venta";
    }
<script>

  

    $(document).ready(function () 
    {
        $("#mediopago").change(function (e) 
        {
                $.get("/Home/SelectMedioPago", { mediopago: e.target.value }, function (response) {
                    $("#divSelectMedioPago").html(response);
                });
           
        });

        });
   

</script>   
        <h2>Venta</h2>


      @if (Model > 0)
      {
        <div id="venta">

            @Html.Label("Subtotal: " + Model) <br><br>

            <form action="../Home/Fin" method="post">
                <div id="divSelecmediopago">
                    @Html.Label("Medio de pago")

                    <select name="mediopago" id="mediopago" >

                        <option value="1">Efectivo</option>
                        <option value="2">Tarjeta</option>
                    </select>
                </div>
                <br />
                <div id="divSelectMedioPago"></div>
                <a href="/Home/Cancelar">Cancelar venta</a>

                <br>
                <input type="submit" value="Finalizar venta" />

            </form>
        </div>
      }
      






